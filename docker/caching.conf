# 브라우저 캐시 설정. Dockerfile 이 /etc/apache2/conf-enabled/ 로 넣는다.
#
# CloudFront 가 이미 캐시하는데 왜 또 거느냐 — 두 층이 하는 일이 다르다.
# CloudFront 는 오리진 부하를 줄이고, Cache-Control 은 브라우저가 재요청
# 자체를 안 하게 한다. 헤더가 없으면 브라우저는 그 응답을 재사용해도 되는지
# 몰라서 페이지를 옮길 때마다 엣지까지 다시 간다. 엣지가 즉시 돌려주니 서버는
# 편하지만 사용자는 매번 왕복을 낸다.
#
# 2026-08 기준 kr/learn 의 정적 자원과 업로드 이미지에 Cache-Control 이 없었다.
# learn 쪽에는 1년이 붙어 있었는데, 그건 예전 SiteGround 호스팅 시절
# SG-Optimizer 가 .htaccess 에 써놓고 플러그인만 지워진 잔재였다. .htaccess 는
# EBS 볼륨 안이라 저장소에 안 남고, 볼륨을 다시 만들면 조용히 사라진다.
# 그래서 이미지 쪽으로 옮겨 양쪽이 같은 설정을 갖게 한다.
#
# CSS·JS 는 넣지 않는다. 워드프레스는 파일명을 그대로 두고 ?ver= 로만 버전을
# 구분하는데, 쿼리는 캐시 키가 아니다. 긴 캐시를 걸면 플러그인을 올려도 옛
# 파일이 계속 나간다. 아래 목록은 내용이 바뀌면 파일명도 바뀌는 것들이다
# (업로드 이미지는 -768x384 처럼 크기가 파일명에 박히고, 서체는 경로에
# 버전이 들어간다 — fonts.conf 참조).
<IfModule mod_expires.c>
    ExpiresActive On

    ExpiresByType image/webp                  "access plus 1 year"
    ExpiresByType image/avif                  "access plus 1 year"
    ExpiresByType image/jpeg                  "access plus 1 year"
    ExpiresByType image/png                   "access plus 1 year"
    ExpiresByType image/gif                   "access plus 1 year"
    ExpiresByType image/svg+xml               "access plus 1 year"
    ExpiresByType image/x-icon                "access plus 1 year"

    ExpiresByType video/mp4                   "access plus 1 year"
    ExpiresByType audio/mpeg                  "access plus 1 year"

    ExpiresByType application/pdf             "access plus 1 year"
</IfModule>
